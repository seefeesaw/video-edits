Here is the key insight. If the total sum is odd, a perfect split is mathematically impossible, so we return false immediately. But if the sum is even, we can rethink the problem entirely: can any subset of the array sum to exactly half the total? For our example, that target is eleven. This is the classic subset sum problem, and we solve it with a boolean DP table that tracks every reachable sum.
