The complete one-dimensional solution. Compute the total sum and immediately return false if it is odd. Set target to total divided by two. Initialize a boolean dp array of size target plus one with only dp zero set to true. For each number, iterate j backwards from target down to num. At each step, update dp j to dp j or dp j minus num. After all numbers are processed, return dp at target.
